# Vite env vars must start with VITE_

# Where QR codes should redirect first (click tracking service).
# This should be a publicly reachable base URL in production.
VITE_CLICK_BASE_URL=https://qr-dragonfly.com
